export const NAME_SESSION_COMMAND_NAME = "name-session";

export const nameSessionCommand = {
  description:
    "为当前 session 生成结构化标题并重命名，提升 session 列表可读性和 find_session 检索召回效果",
  template: `为当前 session 生成一个符合标题协议的结构化标题，并通过 name_session tool 完成重命名。

## 标题协议：\`[动作][对象] 主题\`

格式要求：
- 三部分顺序固定：\`[动作]\` → \`[对象]\` → \`主题\`，中间用空格分隔
- 示例：\`[设计][name_session] 标题协议与实现方案\`

### 动作标签（10 个，必须选择一个）

| 标签     | 适用场景                         |
| -------- | -------------------------------- |
| [调研]   | 技术调研、资料研究、源码阅读     |
| [探索]   | 自由探索、试验性尝试、原型验证   |
| [讨论]   | 方案讨论、需求沟通、问题分析     |
| [设计]   | 架构设计、方案设计、接口设计     |
| [决策]   | 技术选型、方案取舍、方向确定     |
| [实现]   | 功能实现、代码编写、重构         |
| [调试]   | 问题排查、bug 修复、性能调试     |
| [验证]   | 测试验证、结果确认、功能验收     |
| [审查]   | 代码审查、设计审查、安全检查     |
| [交接]   | handoff 写作、上下文打包、知识传递 |

### 对象选取

选取最有检索价值的模块名、功能名、文件名、子系统名或概念名作为 \`[对象]\`，如 \`[core_service]\`、\`[user_layer]\`、\`[config]\`、\`[storage]\`。

不要使用泛泛词汇如 \`[问题]\`、\`[功能]\`、\`[代码]\`、\`[配置]\`。

### 主题约束

- 控制在 8-18 个中文字符
- 表达 session 的最终有效产物
- 保留未来可能用于搜索的关键词
- 不使用完整句子或低信息密度表达
- 不得使用 [TODO]、[意图]、[待办]、[进行中]、[已完成] 等任务管理标签

## 用户命名方向

"""
$ARGUMENTS
"""

（如果非空，agent MUST 据此调整标题的动作标签、对象选取和主题方向。例如用户输入"这是关于 todo 意图实现"表示本 session 是围绕 todo intent 的实现讨论，标题应体现 [实现][todo] 或类似方向。）

## 执行步骤

1. 分析当前 session 的对话内容和核心产出，结合用户的命名方向提示（如有）
2. 按照标题协议生成一个符合规范的标题
3. 调用 \`name_session\` tool 传入 \`title\` 参数完成重命名
4. 完成后提醒用户：当前轮用于重命名标题的对话会对本 session 的任务主线造成噪音污染，建议用户在确认标题后回滚（撤销/删除）本次重命名相关的消息轮次，仅保留标题变更结果`,
} as const;
