import { t } from "../i18n";

export const NAME_SESSION_COMMAND_NAME = "name-session";

export const nameSessionCommand = {
  description: t("zh", "command.name_session.description"),
  template: t("zh", "command.name_session.template"),
} as const;
